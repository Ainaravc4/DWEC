function calcCircumfrence (radio) {
    var circ = Math.PI * 2 * radio;
    alert ("La circunferencia es" + circ);
  }
  function calcArea (radio) {
    var area = Math.PI * radio * radio;
    alert ("El área es" + área);
  }

  calcCircumfrence(5);
  calcArea (10);